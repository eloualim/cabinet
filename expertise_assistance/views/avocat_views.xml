<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        
        <!-- Vue formulaire propre pour les avocats -->
        <record id="view_avocat_form" model="ir.ui.view">
            <field name="name">avocat.form</field>
            <field name="model">avocat</field>
            <field name="arch" type="xml">
                <form string="Avocat">
                    <sheet>
                        <group>
                            <group>
                                <field name="name" string="Nom de l'avocat"/>
                                <field name="arabic_name" placeholder="الاسم بالعربية"/>
                            </group>
                            <group>
                                <field name="phone"/>
                                <field name="mobile"/>
                                <field name="email"/>
                            </group>
                        </group>
                        <notebook>
                            <page name="contact_addresses" string="Contacts &amp; Adresses">
                                <group>
                                    <group string="Adresse">
                                        <field name="street" placeholder="Rue..."/>
                                        <field name="street2"/>
                                        <field name="zip" placeholder="Code postal"/>
                                        <field name="city" placeholder="Ville"/>
                                        <field name="state_id"/>
                                        <field name="country_id"/>
                                    </group>
                                </group>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Vue liste propre pour les avocats -->
        <record id="view_avocat_list" model="ir.ui.view">
            <field name="name">avocat.list</field>
            <field name="model">avocat</field>
            <field name="arch" type="xml">
                <list string="Avocats">
                    <field name="name"/>
                    <field name="arabic_name"/>
                    <field name="city"/>
                    <field name="phone"/>
                    <field name="mobile"/>
                    <field name="email"/>
                </list>
            </field>
        </record>

        <!-- Vue recherche propre pour les avocats -->
        <record id="view_avocat_search" model="ir.ui.view">
            <field name="name">avocat.search</field>
            <field name="model">avocat</field>
            <field name="arch" type="xml">
                <search string="Rechercher Avocats">
                    <field name="name" string="Nom"/>
                    <field name="arabic_name" string="Nom Arabe"/>
                    <field name="city" string="Ville"/>
                    <field name="phone" string="Téléphone"/>
                    <separator/>

                    <group expand="0" string="Grouper par">
                        <filter string="Ville" name="group_city" domain="[]" context="{'group_by': 'city'}"/>
                        <filter string="Pays" name="group_country" domain="[]" context="{'group_by': 'country_id'}"/>
                    </group>
                </search>
            </field>
        </record>

        <!-- Action pour les avocats -->
        <record id="action_avocat" model="ir.actions.act_window">
            <field name="name">Avocats</field>
            <field name="res_model">avocat</field>
            <field name="view_mode">list,form</field>
            <field name="domain">[]</field>
            <field name="context">{}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Ajouter un nouvel avocat
                </p>
                <p>
                    Gérez vos avocats avec leurs informations :
                    nom français et nom arabe.
                </p>
            </field>
        </record>

        <!-- Élément de menu pour avocats -->
        <menuitem id="menu_avocats"
                  name="Avocats"
                  parent="menu_insurance_companies_root"
                  action="action_avocat"
                  sequence="30"/>

    </data>
</odoo>